PHP port of the well known CSS hack described at http://ha.ckers.org/weird/CSS-history.cgi

Note that the anchors / frame containing them must be visible for the browser to send the request to the logging script, so one way to hide the links from an unsuspecting victim would be to use the same color for fg/bg as this example does.